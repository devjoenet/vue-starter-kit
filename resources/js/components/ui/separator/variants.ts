import { tv } from 'tailwind-variants';

export const separatorVariants = tv({ base: 'shrink-0 bg-border data-[orientation=horizontal]:h-px data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-px' });
